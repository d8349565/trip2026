# Domain Docs

This repository uses a single-context layout.

Before engineering work, read root `CONTEXT.md` when present and relevant ADRs under `docs/adr/`. If they do not exist, proceed without blocking. Use the domain vocabulary defined in `CONTEXT.md`, and explicitly flag any proposal that conflicts with an ADR.
